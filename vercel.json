{
  "buildCommand": "npx expo export --platform web",
  "outputDirectory": "dist",
  "regions": ["hnd1"],
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
